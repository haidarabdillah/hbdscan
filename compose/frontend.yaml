version: '3.6'

services:
  explorer:
    image: ahsandev2019/frontend_custom:s1
    restart: always
    depends_on:
      - backend
    env_file:
      - ../config/explorer.env
    volumes:
      - ../config/assets:/app/assets
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.explorer.rule=Host(`www.levccscan.com`)"
      - "traefik.http.routers.explorer.entrypoints=websecure"
      - "traefik.http.routers.explorer.tls=true"
      - "traefik.http.routers.explorer.tls.certresolver=myresolver"
      - "traefik.http.services.explorer.loadbalancer.server.port=3000"
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
